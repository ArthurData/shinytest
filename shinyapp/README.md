# Shinytest

Shinytest is a quick project to try and play with **Shiny for Python.**

I used `pyenv` to manage Python version and libraries.

I launch the app with:

```
shiny run --reload shinyapp/app.py
```
